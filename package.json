{
  "name": "netflix-clone",
  "version": "1.0.0",
  "description": "netflix landing page clone, using html, css, js",
  "default": "index.html",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --dist-dir ./dist"
  },
  "author": "rishi dubey",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "parcel": "^2.0.0-beta.2"
  }
}
